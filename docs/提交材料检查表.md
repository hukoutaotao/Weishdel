# 自走棋对战系统提交材料检查表

日期：2026-08-22

使用方法：`[x]` 表示已经有可追溯证据；`[ ]` 表示仍需学生本人操作或课程平台信息。

## 1. 必须由学生本人填写

- [ ] 在课程报告封面填写班级。
- [ ] 在课程报告封面填写学号。
- [ ] 在课程报告封面填写姓名。
- [ ] 在课程报告封面填写指导教师。
- [ ] 填写后重新打开报告，确认封面内容未挤压、未跨页。

以上四项当前有意留空，不能由程序推测或虚构。

## 2. 源代码与工程文件

- [x] `CMakeLists.txt` 文件名和项目入口正确。
- [x] `CMakePresets.json` 提供 `msvc-debug`、`msvc-release` 配置与构建预设。
- [x] `src/core` 不依赖 SFML，核心规则可独立测试。
- [x] `src/game` 包含 SFML 窗口、页面、渲染、按钮和真人控制器。
- [x] `tests` 包含配置、地图、经济、战斗、技能、对局、运行路径和 AI 测试。
- [x] C++ 标准固定为 C++17，构建器固定为 MSVC x64 + Ninja。
- [x] 没有增加新的项目第三方库；SFML 2.6.2 为既有依赖。
- [ ] 最终提交前确认不包含 `build/qa-temp`、缓存、临时故障副本或个人辅助脚本。

## 3. Release 发布目录

- [x] `dist/AutoChessGame.exe` 存在。
- [x] `dist` 包含 3 个 SFML Release DLL。
- [x] `dist/data` 包含 4 个 cfg 和 2 张地图，共 6 个数据文件。
- [x] `dist` 当前总计 10 个文件，没有开发期日志或调试 DLL。
- [x] 已从项目外工作目录启动并正常关闭。
- [x] EXE 优先读取自身旁边的 `data`，不依赖项目当前工作目录。
- [x] 10.10 已重新执行 `--fresh` Release 构建并重建 `AutoChessDist`。
- [x] 10.10 已重新计算最终 10 个发布文件的 SHA-256。

## 4. 自动测试与证据

- [x] 最终候选测试为 402 PASS / 0 FAIL。
- [x] 18 组 AI 覆盖 3 种策略 × 2 张地图 × 3 个分队。
- [x] 18 组 AI 均 `command_failures=0`、`rounds=3`。
- [x] 固定随机种子为 `20260814`。
- [x] 30 个非法输入样本已记录 SHA-256。
- [x] 已保存 Release 构建、回归、运行、dist 和最终测试日志。
- [x] 10.10 已在最终文档和界面修正后重新生成最终测试日志与证据哈希。

## 5. 课程报告与任务书位置

- [x] 课程报告文件为 `docs/自走棋对战系统课程设计报告.docx`。
- [x] 报告包含摘要、目录、系统需求分析和输入/输出。
- [x] 报告包含总体设计、功能模块图和数据流说明。
- [x] 报告包含详细设计、数据文件、类层次图、界面与模块实现。
- [x] 报告说明抽象类、继承、虚函数多态、多重继承和运算符重载。
- [x] 报告包含 4 个带一句用途注释的关键 C++ 代码块。
- [x] 报告包含测试数据、402 项结果、18 组 AI 结果和故障验收。
- [x] 报告包含 3 个以上“现象—原因—修复—验证”调试案例。
- [x] 报告包含使用说明和编程体会。
- [x] 附录 A 位于正文之后，列出 104 个 C++ 源文件与测试文件。
- [x] 附录 B 位于源程序清单之后，完整嵌入原任务书 4 页。
- [x] 评分表保留在原任务书内，位置位于源程序清单之后。
- [x] Word 目录字段已经更新，不含“按 Ctrl+A、F9”之类占位提示。
- [x] 报告为 28 页，18 张图片均为行内图片且未超过正文宽度。
- [x] 10.10 已再次执行只读报告审计：0 张浮动图片、0 个边缘警告、0 个空白页，最终 AI 截图与内嵌图片一致。
- [ ] 学生填写封面后再次更新目录、页码并检查总页数。

## 6. 图示与运行截图

- [x] 系统功能模块图 1 张。
- [x] 核心类层次图 1 张。
- [x] 完整对局流程图 1 张。
- [x] 11 张唯一 Release 运行截图覆盖主菜单到继续战斗。
- [x] 图示和截图资源清单记录来源、用途与 SHA-256。
- [x] 两组哈希重复的临时截图未进入正式报告。
- [x] AI 选择页截图来自修改后真实 Release 程序，并使用最终文案。

## 7. 独立说明文档

- [x] `docs/使用说明.md`：从双击 `dist` 到完成一局，并给出准确构建、测试、Dist 命令。
- [x] `docs/调试记录.md`：至少 3 个真实案例，统一使用“现象—原因—修复—验证”。
- [x] `docs/提交材料检查表.md`：逐项映射任务书、报告、发布、测试和演示材料。
- [x] `docs/演示视频录制脚本.md`：给出从隐私检查、双击 EXE 到完整一局及播放、大小、SHA-256 验收的逐步动作。
- [x] `docs/config-format.md`：正式 cfg/map 格式和字段说明。

## 8. 演示视频

- [ ] 录制一局从双击 EXE、主菜单、选择、购买、部署、战斗到结果页的连续视频。
- [ ] 视频中至少展示一次帮助页、路线显示、暂停/继续和技能按钮状态。
- [ ] 视频画面能看清 1280×720 游戏窗口，鼠标位置和按钮反馈可辨认。
- [ ] 视频无个人隐私窗口、无调试临时目录、无无关通知遮挡。
- [ ] 视频文件可在另一台播放器中从头播放到结尾。
- [ ] 视频文件名、时长、大小和 SHA-256 在学生录制并完整播放验收后登记。

在没有实际录制和播放验证前，这一节不得提前勾选。

## 9. 任务书要求与证据对应

| 任务书要求 | 证据位置 | 当前状态 |
|---|---|---|
| 系统需求分析、流程图、输入输出 | 报告第 1 章、图 1-1 | 已完成 |
| 总体设计、功能模块图 | 报告第 2 章、图 2-1 | 已完成 |
| 详细设计、数据文件、类层次图 | 报告第 3 章、图 3-1 | 已完成 |
| 抽象类、继承、虚函数多态 | 报告 3.2、4.2 和附录源程序 | 已完成 |
| 多重继承、运算符重载 | 报告 3.2、3.3、4.2 | 已完成 |
| 界面和模块实现 | 报告第 4、6 章及 11 张截图 | 已完成 |
| 系统调试 | 报告第 7 章、`docs/调试记录.md` | 已完成 |
| 使用说明、编程体会 | 报告第 8 章、`docs/使用说明.md` | 已完成 |
| 关键源程序带注释 | 报告第 4 章、附录 A | 已完成 |
| 文件测试数据和测试结果 | `tests/data`、`docs/test-results` | 已完成 |
| 完整任务书和评分表 | 报告附录 B | 已完成 |
| Release 程序和 DLL | `dist` | 已于 10.10 重建并确认 |
| 完整一局演示视频 | 最终提交视频文件；操作指引见 `docs/演示视频录制脚本.md` | 尚未录制 |

## 10. 提交前最后一次人工检查

- [ ] 把最终提交材料复制到一个新目录，不包含 `.git` 和 `build`。
- [ ] 在新目录中双击 `dist/AutoChessGame.exe`，完成至少一局。
- [ ] 打开报告，检查封面、目录、全部图片、源程序清单、任务书和评分表。
- [ ] 打开三份 Markdown 说明，确认中文编码正常、命令可复制。
- [ ] 完整播放演示视频。
- [ ] 对照课程平台限制检查压缩包名称、格式和大小。
- [ ] 压缩后重新解压到另一个目录，再次双击程序并抽查报告、视频。

只有本节全部完成后，才可将压缩包上传为最终提交。
