# 第 10 天执行记录

日期：2026-08-22

## 10.1 第10天基线

- 第 1～9 天计划已完成，当前基线提交为 `d6cbffb`（`9.10 完成18组AI对局集成测试`）。
- 当前分支：`步骤2`。
- 今日只处理稳定性、自动测试、Release 发布、文档、演示材料和最终审计。
- 不增加新玩法，不改变冻结规则，不引入新的第三方库。
- 仓库根目录的 `_inspect_db.py`、`_inspect_db2.py`、`_search_log.py` 为既有未跟踪辅助文件，本日提交不包含这些文件。

## 今日验收门槛

1. MSVC x64 Release 配置和编译成功。
2. Release 自动测试全部通过，18 组 AI 组合均有可追溯结果。
3. 发布目录包含程序、SFML Release DLL、配置和地图数据。
4. 程序脱离开发构建目录后可启动并完成对局验证。
5. 最终测试输入、日志、报告材料、使用说明和调试记录齐全。
6. 每个步骤自检通过后单独提交，不把多个未验证步骤混入同一提交。

## 缺陷处理规则

- P0：Release 不能构建、程序不能启动、崩溃、死循环、不能完成对局、发布目录缺运行文件。立即停止后续工作并修复。
- P1：明确规则错误、测试失败、报告规定内容缺失。发布冻结前修复。
- P2：不影响运行和验收的显示问题。仅在修改范围小且能够完成回归测试时处理。

## 分步记录

- [x] 10.1 建立第10天基线。
- [x] 10.2 干净配置并编译 Release x64。
- [x] 10.3 运行完整自动测试并保存结果。
- [x] 10.4 复核重点规则与18组AI组合。
- [x] 10.5 执行可自动化的人工验收与故障启动检查。
- [x] 10.6 组装并验证独立 dist 发布目录。
- [ ] 10.7 固定最终测试输入、日志和版本信息。
- [ ] 10.8 完成截图、结构图和报告材料。
- [ ] 10.9 完成使用说明、调试记录及提交材料检查。
- [ ] 10.10 准备演示视频材料并执行最终提交审计。
## 10.2 Release x64 构建自检

- 使用 Visual Studio 2026 Developer Command Prompt，目标架构为 x64。
- CMake 3.29.2、Ninja 1.12.0、MSVC 19.51.36256.0。
- 通过 `msvc-release` 预设执行 `--fresh` 配置，构建类型确认是 Release。
- CMake 找到项目现有的 SFML 2.6.2，未下载或增加第三方依赖。
- Ninja 完成 47/47 个构建步骤，退出码为 0。
- 构建日志中编译错误、链接错误和 MSVC 警告均为 0。
- 生成 `AutoChessGame.exe`、`AutoChessTests.exe`、`AutoChessCore.lib`。
- 游戏目录已复制 `sfml-graphics-2.dll`、`sfml-window-2.dll`、`sfml-system-2.dll`。
- 完整构建日志：`docs/test-results/release-build-2026-08-22.log`。
## 10.3 Release 自动测试自检

- 运行 `build/msvc-release/AutoChessTests.exe`，进程退出码为 0。
- 共统计到 395 条 PASS、0 条 FAIL。
- 日志中没有未处理异常、终止、崩溃或非零 AI 命令失败。
- 完整对局脚本输出 3 条回合摘要和 1 条最终对局摘要。
- AI 集成矩阵输出 18 组结果，覆盖 3 种策略、2 张地图和 3 个分队。
- 18 组 AI 场景的 `command_failures` 全部为 0，均在 3 回合正常结束。
- 正式配置摘要记录随机种子为 `20260814`。
- 原始测试日志：`docs/test-results/release-tests-2026-08-22.log`。
## 10.4 重点规则与AI矩阵自检

- 对总计划最高优先级清单逐项建立了“规则—测试名称—结果”对应表。
- 发现普通攻击已有首次入圈帧规则测试，但缺少“同帧按较小编号”直接断言，因此只补充该测试，不修改生产代码。
- 新增测试代码块带有一句用途注释，符合项目现有测试书写方式。
- Release 重新构建通过，新增测试运行通过，总结果为 396 PASS / 0 FAIL。
- 18 组 AI 场景与上一次输出逐行一致，全部 `command_failures=0`。
- 规则矩阵：`docs/test-results/priority-regression-matrix-2026-08-22.md`。
- 回归日志：`docs/test-results/release-regression-2026-08-22.log`。
## 10.5 运行与故障验收自检

- Release 程序成功创建 `AutoChess` 窗口，存活检查通过，接收关闭消息后退出码为 0。
- 在 192 DPI（200% 缩放）环境中，以 1280×720 真实客户区依次完成主菜单、帮助、地图、分队、AI、购买、部署、战斗、暂停和继续流程。
- 13 张临时截图得到 11 个唯一 SHA-256；返回主菜单前后截图一致，暂停 1.5 秒前后截图一致，继续后的截图发生变化。
- 五类故障启动检查全部命中预期：缺配置、缺地图、非法 `max_rounds`、缺字体均以退出码 1 受控结束；缺 SFML DLL 以 `0xC0000135` 结束。
- 所有故障样本均位于带所有权标记的 `build/qa-temp` 隔离目录，没有改动生产数据。
- 临时截图：`build/qa-temp/autochess-10.5-accepted`；临时故障样本：`build/qa-temp/fault-injection-03c9a5f3cfb84326a82d9dbd9504da8f`。
- 正式日志：`docs/test-results/runtime-acceptance-2026-08-22.log`。
- 本步骤未修改生产代码、未引入第三方库。
## 10.6 独立发布目录自检

- 新增可独立测试的运行路径工具：优先使用 EXE 旁 `data`，仅在该目录不存在时回退开发数据目录。
- `GameApp` 通过构造函数接收选定目录，不再直接依赖编译期源码绝对路径。
- 新增 5 个路径断言并运行完整 Release 测试，结果为 402 PASS / 0 FAIL。
- CMake `AutoChessDist` 目标组装出 10 个必要文件：1 个 EXE、3 个 SFML Release DLL、4 个 cfg 和 2 张地图。
- dist 中 6 个数据文件与正式 `data` 的 SHA-256 全部一致，没有多余文件。
- 将 dist 复制到项目外 `D:\AutoChess-dist-10.6-bee38c877fd047a89016f740469ce9c7`，并以 `D:\` 为工作目录启动；窗口标题为 `AutoChess`，正常关闭退出码为 0。
- 外部副本临时缺少 `game.cfg` 时，错误明确指向外部 EXE 旁的 `data`，证明没有回退读取 `D:\1\data`；验证后已恢复临时文件并清理项目外副本。
- 正式日志：`docs/test-results/dist-verification-2026-08-22.log`。
- 本步骤没有引入第三方库，没有增加玩法，没有修改冻结规则。
