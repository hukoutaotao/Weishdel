# 第 10 天执行记录

日期：2026-08-22

## 10.1 第10天基线

- 第 1～9 天计划已完成，当前基线提交为 `d6cbffb`（`9.10 完成18组AI对局集成测试`）。
- 当前分支：`步骤2`。
- 今日只处理稳定性、自动测试、Release 发布、文档、演示材料和最终审计。
- 不增加新玩法，不改变冻结规则，不引入新的第三方库。
- 仓库根目录的 `_inspect_db.py`、`_inspect_db2.py`、`_search_log.py` 为既有未跟踪辅助文件，本日提交不包含这些文件。

## 今日验收门槛

1. MSVC x64 Release 配置和编译成功。
2. Release 自动测试全部通过，18 组 AI 组合均有可追溯结果。
3. 发布目录包含程序、SFML Release DLL、配置和地图数据。
4. 程序脱离开发构建目录后可启动并完成对局验证。
5. 最终测试输入、日志、报告材料、使用说明和调试记录齐全。
6. 每个步骤自检通过后单独提交，不把多个未验证步骤混入同一提交。

## 缺陷处理规则

- P0：Release 不能构建、程序不能启动、崩溃、死循环、不能完成对局、发布目录缺运行文件。立即停止后续工作并修复。
- P1：明确规则错误、测试失败、报告规定内容缺失。发布冻结前修复。
- P2：不影响运行和验收的显示问题。仅在修改范围小且能够完成回归测试时处理。

## 分步记录

- [x] 10.1 建立第10天基线。
- [x] 10.2 干净配置并编译 Release x64。
- [x] 10.3 运行完整自动测试并保存结果。
- [x] 10.4 复核重点规则与18组AI组合。
- [x] 10.5 执行可自动化的人工验收与故障启动检查。
- [x] 10.6 组装并验证独立 dist 发布目录。
- [x] 10.7 固定最终测试输入、日志和版本信息。
- [x] 10.8 完成截图、结构图和报告材料。
- [x] 10.9 完成使用说明、调试记录及提交材料检查。
- [x] 10.10 准备演示视频材料并执行最终提交审计。
## 10.2 Release x64 构建自检

- 使用 Visual Studio 2026 Developer Command Prompt，目标架构为 x64。
- CMake 3.29.2、Ninja 1.12.0、MSVC 19.51.36256.0。
- 通过 `msvc-release` 预设执行 `--fresh` 配置，构建类型确认是 Release。
- CMake 找到项目现有的 SFML 2.6.2，未下载或增加第三方依赖。
- Ninja 完成 47/47 个构建步骤，退出码为 0。
- 构建日志中编译错误、链接错误和 MSVC 警告均为 0。
- 生成 `AutoChessGame.exe`、`AutoChessTests.exe`、`AutoChessCore.lib`。
- 游戏目录已复制 `sfml-graphics-2.dll`、`sfml-window-2.dll`、`sfml-system-2.dll`。
- 完整构建日志：`docs/test-results/release-build-2026-08-22.log`。
## 10.3 Release 自动测试自检

- 运行 `build/msvc-release/AutoChessTests.exe`，进程退出码为 0。
- 共统计到 395 条 PASS、0 条 FAIL。
- 日志中没有未处理异常、终止、崩溃或非零 AI 命令失败。
- 完整对局脚本输出 3 条回合摘要和 1 条最终对局摘要。
- AI 集成矩阵输出 18 组结果，覆盖 3 种策略、2 张地图和 3 个分队。
- 18 组 AI 场景的 `command_failures` 全部为 0，均在 3 回合正常结束。
- 正式配置摘要记录随机种子为 `20260814`。
- 原始测试日志：`docs/test-results/release-tests-2026-08-22.log`。
## 10.4 重点规则与AI矩阵自检

- 对总计划最高优先级清单逐项建立了“规则—测试名称—结果”对应表。
- 发现普通攻击已有首次入圈帧规则测试，但缺少“同帧按较小编号”直接断言，因此只补充该测试，不修改生产代码。
- 新增测试代码块带有一句用途注释，符合项目现有测试书写方式。
- Release 重新构建通过，新增测试运行通过，总结果为 396 PASS / 0 FAIL。
- 18 组 AI 场景与上一次输出逐行一致，全部 `command_failures=0`。
- 规则矩阵：`docs/test-results/priority-regression-matrix-2026-08-22.md`。
- 回归日志：`docs/test-results/release-regression-2026-08-22.log`。
## 10.5 运行与故障验收自检

- Release 程序成功创建 `AutoChess` 窗口，存活检查通过，接收关闭消息后退出码为 0。
- 在 192 DPI（200% 缩放）环境中，以 1280×720 真实客户区依次完成主菜单、帮助、地图、分队、AI、购买、部署、战斗、暂停和继续流程。
- 13 张临时截图得到 11 个唯一 SHA-256；返回主菜单前后截图一致，暂停 1.5 秒前后截图一致，继续后的截图发生变化。
- 五类故障启动检查全部命中预期：缺配置、缺地图、非法 `max_rounds`、缺字体均以退出码 1 受控结束；缺 SFML DLL 以 `0xC0000135` 结束。
- 所有故障样本均位于带所有权标记的 `build/qa-temp` 隔离目录，没有改动生产数据。
- 临时截图：`build/qa-temp/autochess-10.5-accepted`；临时故障样本：`build/qa-temp/fault-injection-03c9a5f3cfb84326a82d9dbd9504da8f`。
- 正式日志：`docs/test-results/runtime-acceptance-2026-08-22.log`。
- 本步骤未修改生产代码、未引入第三方库。
## 10.6 独立发布目录自检

- 新增可独立测试的运行路径工具：优先使用 EXE 旁 `data`，仅在该目录不存在时回退开发数据目录。
- `GameApp` 通过构造函数接收选定目录，不再直接依赖编译期源码绝对路径。
- 新增 5 个路径断言并运行完整 Release 测试，结果为 402 PASS / 0 FAIL。
- CMake `AutoChessDist` 目标组装出 10 个必要文件：1 个 EXE、3 个 SFML Release DLL、4 个 cfg 和 2 张地图。
- dist 中 6 个数据文件与正式 `data` 的 SHA-256 全部一致，没有多余文件。
- 将 dist 复制到项目外 `D:\AutoChess-dist-10.6-bee38c877fd047a89016f740469ce9c7`，并以 `D:\` 为工作目录启动；窗口标题为 `AutoChess`，正常关闭退出码为 0。
- 外部副本临时缺少 `game.cfg` 时，错误明确指向外部 EXE 旁的 `data`，证明没有回退读取 `D:\1\data`；验证后已恢复临时文件并清理项目外副本。
- 正式日志：`docs/test-results/dist-verification-2026-08-22.log`。
- 本步骤没有引入第三方库，没有增加玩法，没有修改冻结规则。
## 10.7 最终测试证据自检

- 程序候选提交固定为 `967915dc8b7f0399046fb04c8a1d9dacbf38c935`（`10.6 完成独立发布目录`）。
- 针对该候选重新运行 Release 测试，退出码为 0，结果为 402 PASS / 0 FAIL。
- 固定随机种子为 `20260814`，来源于 `dist/data/game.cfg`。
- 最终 AI 矩阵正好 18 行，覆盖 3 种策略、2 张地图和 3 个分队；全部 `command_failures=0`、`rounds=3`。
- 证据清单记录 10 个 dist 文件、30 个非法输入样本、最终日志和测试程序的 SHA-256。
- 最终测试日志：`docs/test-results/final-release-tests-2026-08-22.log`。
- 最终证据清单：`docs/test-results/final-evidence-2026-08-22.md`。
- 本步骤未修改生产代码、测试判定或第三方依赖。

## 10.8 报告与图示材料自检

- 正式资源目录包含 3 张项目图示：系统功能模块图、核心类层次图和完整对局流程图。
- 从 Release x64 实际运行的 13 张验收截图中保留 11 张唯一画面，覆盖主菜单、帮助、地图、分队、AI、准备、购买、部署、战斗、暂停和继续。
- 课程设计报告为 `docs/自走棋对战系统课程设计报告.docx`，Word 更新目录和字段后共 28 页；目录已生成，不含待更新占位提示。
- 报告包含需求分析、输入输出、总体设计、详细设计、系统实现、测试、调试、使用说明、编程体会、4 个带用途注释的关键 C++ 代码块，以及抽象类、继承、虚函数多态、多重继承和运算符重载说明。
- 附录 A 的源程序清单共 104 项；附录 B 位于源程序清单之后，整页嵌入原任务书 4 页，评分表随原任务书保留在规定位置。
- DOCX 结构检查得到 18 张行内图片、0 张浮动图片、0 张超出正文宽度的图片；18 张图片由 3 张图示、11 张运行截图和 4 页任务书组成。
- Word 导出的 PDF 与逐页 PNG 均为 28 页；逐页边界检查无内容触边、无空白页，所有 PNG 均为 1191×1684。
- 正式资源清单记录每张图示和截图的来源、用途及 SHA-256；两组重复截图未进入正式报告。
- 本步骤未修改生产代码、配置或发布目录，未引入项目第三方依赖；Pillow 仅用于生成报告图片，不参与游戏构建和发布。

## 10.9 使用说明与提交文档自检

- 新增 `docs/使用说明.md`、`docs/调试记录.md` 和 `docs/提交材料检查表.md`，分别覆盖新手运行步骤、4 个真实调试案例及任务书证据映射。
- 构建、测试和发布命令已逐项对照 `CMakePresets.json`，统一使用真实预设 `msvc-release`；每个 Markdown 命令代码块首行均有一句“作用”注释。
- 编写文档时发现 AI 选择页仍保留第 9 天占位文案，并发现报告曾使用不存在的 `build-msvc-release`；现已改为最终 AI 说明和正确预设名。
- 在 Visual Studio 2026 x64 开发环境中重新完成 Release 构建；自动测试结果为 402 PASS / 0 FAIL，18 组 AI MATRIX 全部 `command_failures=0`、`rounds=3`。
- 通过真实 Release 程序依次点击“开始游戏—第一张地图—第一个分队”进入 AI 选择页，重新截取 1280×720 客户区；正式截图 SHA-256 为 `94F418FB9DDB15DF192184E224528F0EBA9F5AA6514983249510B68FACD05939`，11 张正式截图仍有 11 个唯一哈希。
- 重新生成课程报告并由 Word 更新目录、字段和页码；DOCX、PDF 与逐页 PNG 均为 28 页，包含 18 张行内图片、0 张浮动图片、0 张超宽图片、0 个边缘警告和 0 个空白页，附录 A 仍列出 104 项源程序。
- 报告正文中 `build-msvc-release` 出现次数为 0，正确的 `msvc-release` 出现 5 次；最终 AI 截图以 `word/media/image8.png` 原样嵌入报告。
- 本步骤未引入新的项目第三方库；封面班级、学号、姓名、指导教师以及完整一局演示视频仍必须由本人填写或录制，不提前标记为完成。
## 10.10 最终提交审计自检

- 最终生产候选固定为 `69940461c03a75df7dac8ff71a0fdd37577920be`，标题为 `10.9 完成使用说明与提交文档`。
- 在 Visual Studio 2026 x64 开发环境中重新执行 `cmake --preset msvc-release --fresh` 和 `cmake --build --preset msvc-release`，两条命令退出码均为 0。
- 本轮全新 Release 构建日志为 `docs/test-results/final-release-build-2026-08-22.log`，未下载或增加第三方依赖。
- 重新运行 `build/msvc-release/AutoChessTests.exe`，退出码为 0，统计为 402 PASS / 0 FAIL，标准错误为空。
- 最终日志包含且只包含 18 行 AI MATRIX；每行均为 `command_failures=0`、`rounds=3`，固定种子为 `20260814`。
- 重新执行 `AutoChessDist`，`dist` 恰好包含 10 个文件：1 个 EXE、3 个 SFML Release DLL、4 个 cfg、2 张地图。
- 六个 `dist/data` 文件与源码 `data` 对应文件 SHA-256 全部一致；重建后的 `dist/AutoChessGame.exe` 已包含 10.9 的最终界面文案。
- 将 `dist` 复制到 `D:\1` 之外的唯一临时目录，以 `D:\` 为工作目录启动；成功创建 `AutoChess` 窗口，正常关闭并以退出码 0 结束。
- 外部临时目录使用随机唯一名称和所有权标记；清理前核对绝对路径与标记，只删除本轮创建的目录。
- `docs/test-results/final-evidence-2026-08-22.md` 已替换旧 10.6 候选，记录本轮日志、测试程序、10 个 dist 文件、30 个非法输入样本和 18 组 AI 结果的实际哈希。
- 最终报告只读审计仍为 28 页、18 张行内图片、0 张浮动图片、0 个超宽图片、0 个边缘警告、0 个空白页；最终 AI 截图与报告内嵌 `word/media/image8.png` 哈希一致。
- 报告封面的班级、学号、姓名和指导教师继续留空，没有推测或虚构个人信息。
- 本机已检测到 Xbox Game Bar 和 Windows 截图工具，但完整一局录制涉及隐私检查和人工 GUI 操作，未伪造视频文件或播放结论。
- 已新增 `docs/演示视频录制脚本.md`，逐步列出双击 EXE、帮助、选择、购买、部署、路线、战斗、技能状态、暂停、继续、结果页及录制后播放和哈希验收。
- 演示视频相关检查项继续保持未勾选；必须由学生本人录制、完整播放并登记文件名、时长、大小和 SHA-256 后才能完成。
- 本步骤没有修改生产源码和测试判定，没有引入新第三方库。