# 第 7 天完成清单

日期：2026-08-18

## 今日目标

- [x] 玩家可以使用鼠标从主菜单进入地图、分队和电脑策略选择流程。
- [x] 准备阶段能够显示地图、路线、双方部署、经济信息、商店、备用区和死亡列表。
- [x] 购买、刷新、部署、撤回、合成、出售、复活和提前开战均通过 `GameCommand` 提交给核心层。
- [x] 界面只读取 `ReadOnlyGameView`，没有直接修改核心对局对象。
- [x] 战斗、回合结算和对局结果均有明确的阶段占位显示，为第 8 天战斗界面保留边界。
- [x] 未引入新的第三方库，继续使用 C++17、SFML 2.6.2 和项目现有依赖。

## 分步提交

- [x] `7d10ba2`：`7.1 建立第7天基线`
- [x] `a6cf990`：`7.2 建立应用运行骨架`
- [x] `78c5d00`：`7.3 完成菜单帮助框架`
- [x] `245c3b0`：`7.4 接通开局选择流程`
- [x] `ad1b2d5`：`7.5 绘制地图与路线`
- [x] `16aea70`：`7.6 完成准备信息商店`
- [x] `1ceca0f`：`7.7 实现单位基础拖拽`
- [x] `ba56588`：`7.8 接入合成出售反馈`
- [x] `47410a7`：`7.9 接入复活开战边界`
- [x] `7.10 完成全量验收`：由本清单所在提交完成。

## 功能验收

### 应用和导航

- [x] 创建 1280×720 SFML 窗口并使用固定 1/60 秒更新步长。
- [x] 中文字体按微软雅黑、黑体、等线顺序回退，全部失败时显示明确错误。
- [x] 主菜单提供开始游戏、帮助和退出按钮。
- [x] 帮助页说明选择、购买、拖拽、合成、出售、复活和开战操作。
- [x] 地图、分队和电脑策略选择均由按钮生成统一核心命令。

### 地图和准备界面

- [x] 绘制地形、障碍、双方部署格、双方守卫点和预定义路线。
- [x] 路线按钮可以在显示与隐藏状态之间切换。
- [x] HUD 显示回合、金币、双方守卫和准备倒计时。
- [x] 六槽商店显示单位名称、等级和核心给出的价格。
- [x] 刷新按钮显示配置中的刷新费用并提交刷新命令。
- [x] 八个备用槽显示己方单位，地图显示双方公开部署单位。

### 鼠标准备操作

- [x] 点击商店卡片提交购买命令，已购买槽位显示禁用状态。
- [x] 单位可以从备用区拖到己方合法部署格，也可以拖回指定备用槽。
- [x] 单位拖到已占用的备用槽或部署格时提交合成命令。
- [x] 单位拖到红色出售区时提交出售命令。
- [x] 单位拖到无效区域时取消拖拽并显示三秒中文提示。
- [x] 商店与死亡列表通过右侧页签互斥显示。
- [x] 死亡列表最多显示八个复活按钮，点击后按持久单位 ID 提交复活命令。
- [x] 电脑没有部署单位时开战按钮禁用并显示“电脑正在准备”。
- [x] 电脑完成部署后开战按钮提交 `StartCombatCommand`，核心继续验证真人部署条件。

### 阶段边界

- [x] `Preparation` 显示完整准备操作界面。
- [x] `Combat` 显示地图和“第 8 天接入完整战斗界面”提示。
- [x] `RoundSettlement` 显示明确的回合结算提示。
- [x] `MatchResult` 根据核心结果显示胜、负或平局及最终守卫摘要。
- [x] 第 7 天占位电脑只负责合法选队、购买和部署，三种正式 AI 策略仍留到第 9 天。

## 自动验证结果

| 验证项 | 结果 |
| --- | --- |
| Debug 清理后完整构建 | 通过，41 个编译或链接步骤完成 |
| Debug `AutoChessTests` | 通过，360 PASS / 0 FAIL |
| Release 全新 CMake 配置 | 通过，MSVC x64，识别 SFML 2.6.2 |
| Release 完整构建 | 通过，41 个编译或链接步骤完成 |
| Release `AutoChessTests` | 通过，360 PASS / 0 FAIL |
| Debug 游戏启动冒烟 | 通过，启动三秒内进程保持运行 |
| Release 游戏启动冒烟 | 通过，启动三秒内进程保持运行 |
| 鼠标事件链冒烟 | 通过，依次发送开始、三项选择、购买、拖拽部署和开战事件，进程保持运行 |
| Git 差异格式检查 | 通过，无尾随空白或补丁格式错误 |

## VS Code 人工复核路径

自动测试可以验证核心规则和程序稳定性，但提交课程项目前仍建议在可见窗口中按以下顺序观察一次布局和中文显示：

1. 使用 CMake Tools 选择 `msvc-debug` 并启动 `AutoChessGame`。
2. 点击“帮助”，确认六条说明完整显示，再返回主菜单。
3. 点击“开始游戏”，依次选择第一张地图、第一个分队和任一电脑策略。
4. 等待电脑完成购买和部署，确认“电脑正在准备”变为“提前开始战斗”。
5. 购买两个相同单位，分别检查部署、撤回、合成、出售和无效投放提示。
6. 在后续回合切换到死亡列表，点击死亡单位检查复活结果和金币变化。
7. 至少部署一个己方单位并点击“提前开始战斗”，确认出现第 8 天战斗界面提示。

## 第 8 天入口

- 战斗阶段继续读取 `battleUnits`，绘制双方单位、生命值、技力值和目标。
- 将技能选择与 `ReleaseSkillCommand` 接通，不在渲染层计算伤害或技力。
- 增加暂停、继续、回合结算停留和最终结果页面操作。
